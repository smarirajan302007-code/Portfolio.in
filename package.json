{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "Portfolio full-stack app - root workspace",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "install:all": "npm install --prefix server && npm install --prefix client",
    "seed": "npm run seed --prefix server",
    "vercel-build": "npm install --prefix server && npm install --prefix client && npm run build --prefix client && rm -rf dist public && cp -r client/dist dist && cp -r client/dist public"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  },
  "dependencies": {
    "mongodb": "^7.5.0"
  }
}
